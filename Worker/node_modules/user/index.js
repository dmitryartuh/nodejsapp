/**
 * Created by User on 22.11.2015.
 */
module.exports.create_user = function(id, socket){
    var user = {};
    user.id = id;

    user.game_session = null;

    user.socket = socket;

    return user;
}